config:
    renderer: markdown

modules:
  workshop-environment:
    name: Your Workshop Environment
  setup-environment:
    name: Setup  Environment via CodeReady Workspaces
  data-cleansing:
    name: Data Cleansing in OpenShift
  model-training:
    name: Training a Model in OpenShift
  model-running:
    name: Running the Model
  model-updates:
    name: Updating the Model
